apiVersion: v1
kind: Service
metadata:
  name: {{ include "wisecow.fullname" . }}
  labels:
    {{- include "wisecow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "wisecow.selectorLabels" . | nindent 4 }}
